SELECT * FROM superstore 
ORDER BY price;
ORDERING ALL DATA FROM SUPERSTORE BY PRICE

SELECT SUM(price), AVG(price), MIN(price),
MAX(price), COUNT(price) 
FROM superstore
CREATED A CHART SHOWING THE SUM,AVG,MAX,MIN, AND COUNT OF ALL DATA IN SUPERSTORE

SELECT price FROM superstore
WHERE category='Kitchen Supplies'
THE PRICING OF THE CATERGORY KITCHEN SUPPLIES

SELECT * FROM superstore
WHERE price < 100 AND price > 30
SHOWS PRICES OF ITEMS BELOW 100 AND ABOVE 30

